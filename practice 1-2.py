def max3(a, b, c):
    maximum = a
    if b > maximum: maximum = b
    if c > maximum: maximum = c
    return maximum

#알고리즘 - 어떠한 문제를 해결하기 위해 정해 놓은 일련의 절차
#올바른 알고리즘 - 어떠한 경우에도 실행 결과가 똑같이 나오는 것
#복합문의 스위트는 반ㄴ드시 행마다 같은 수준으로 들여쓰기를 해야함
#만약 스위트가 잔순문이면 헤더와 같은 행에 둘 수 있음
#스위트가 복합문이면 헤더와 스위트를 같은 행에 포함시킬 수 없음